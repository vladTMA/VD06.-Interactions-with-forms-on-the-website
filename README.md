

# VD06.-Взаимодействия с формами на сайте

Простой учебный проект на **Flask**, демонстрирующий работу с HTML‑формами, Bootstrap и обработкой данных на стороне сервера.

## 🚀 Возможности
- Форма для ввода данных пользователя: **имя, возраст, город, хобби**.
- Валидация данных на клиенте (HTML5) и сервере (Flask).
- Отображение введённых данных в карточках Bootstrap на той же странице.
- Обработка ошибок: при некорректном вводе значения сохраняются и выводятся сообщения.

## 📂 Структура проекта
```
project/
├─ app/
│  ├─ __init__.py        # инициализация Flask
│  ├─ routes.py          # маршруты и логика обработки формы
│  └─ templates/
│     └─ form.html       # HTML-шаблон с формой и результатами
├─ main.py               # точка входа
├─ requirements.txt      # зависимости
└─ README.md             # документация
```

## ⚙️ Установка и запуск

1. Клонируйте репозиторий или скачайте файлы.
2. Создайте виртуальное окружение:
   ```bash
   python -m venv .venv
   source .venv/bin/activate   # macOS/Linux
   .venv\Scripts\activate      # Windows
   ```
3. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```
4. Запустите приложение:
   ```bash
   python main.py
   ```
5. Перейдите в браузере по адресу:  
   [http://127.0.0.1:5000](http://127.0.0.1:5000)

## 🧪 Тестирование
- Введите корректные данные — они появятся в карточках.
- Попробуйте ввести некорректный возраст (например, `-5` или `200`) — появится сообщение об ошибке.
- При ошибках введённые значения сохраняются в полях формы.

## 🔮 Возможные улучшения
- Подключение **Flask-WTF** для удобной работы с формами и CSRF‑защиты.
- Использование **SQLite/SQLAlchemy** для хранения данных.
- Добавление авторизации и регистрации пользователей.
- Расширение UI с помощью Bootstrap‑компонентов.

---

✍️ Проект создан в рамках учебного задания по Flask.  
```

